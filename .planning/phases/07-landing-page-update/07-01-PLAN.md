---
phase: 07-landing-page-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/index.html
  - docs/index.html
autonomous: true
requirements:
  - LAND-01
  - LAND-02

must_haves:
  truths:
    - "Landing page describes General Notes as a distinct feature with privacy emphasis and one-click push workflow"
    - "Landing page mentions agent-to-agent collaboration tools (Form I commission split)"
    - "Area Guides section is a brief feature mention, not 10 full community profile cards"
    - "Nav links match actual page sections (no orphaned anchors)"
    - "Section background alternation (white/gray) is consistent from top to bottom"
    - "landing/index.html and docs/index.html are identical after all edits"
  artifacts:
    - path: "landing/index.html"
      provides: "Updated landing page with v1.1 feature descriptions"
      contains: "General Notes"
    - path: "docs/index.html"
      provides: "Synced copy of landing page for GitHub Pages"
      contains: "General Notes"
  key_links:
    - from: "nav links"
      to: "section ids"
      via: "href=#anchor matching id=anchor"
      pattern: "href=\"#[a-z]+\""
    - from: "landing/index.html"
      to: "docs/index.html"
      via: "file copy"
      pattern: "identical content"
---

<objective>
Update the landing page to accurately describe the full v1.1 feature set: expand the General Notes description into a prominent feature callout, verify agent-to-agent collaboration tools (Form I) are adequately mentioned, trim the Area Guides section from 10 full community profiles to a brief feature mention, and sync the result to docs/.

Purpose: The landing page currently describes v1.0 features. v1.1 shipped General Notes and rewrote Form I templates. The landing page must reflect the shipped product so prospective users understand the full value.

Output: Updated landing/index.html and docs/index.html with consistent v1.1 feature descriptions.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-landing-page-update/07-CONTEXT.md
@.planning/phases/07-landing-page-update/07-RESEARCH.md

<interfaces>
<!-- Landing page structure and patterns the executor needs to understand -->

Current section order in landing/index.html (1,657 lines):
  NAV (line 574) -> HERO -> POPUP DEMO -> FEATURES (line 656) -> SCREENSHOTS (line 738)
  -> TEMPLATES -> TRANSCRIBER -> FORMS -> AREA GUIDES (line 1050) -> EDUCATION (line 1252)
  -> FAQ -> GETTING STARTED -> DOWNLOAD -> CTA -> FOOTER -> LIGHTBOX -> SCRIPT

Nav links (line 577-584):
```html
<ul class="nav-links">
  <li><a href="#demo">How It Works</a></li>
  <li><a href="#features">Features</a></li>
  <li><a href="#templates">Templates</a></li>
  <li><a href="#areas">Areas</a></li>
  <li><a href="#education">Education</a></li>
  <li><a href="#download">Coming Soon</a></li>
</ul>
```

Feature card pattern (used in Features Grid, line 662-733):
```html
<div class="feature-cell">
  <div class="feature-icon">[emoji HTML entity]</div>
  <h3>[Feature Name]</h3>
  <p>[1-2 sentence description]</p>
</div>
```

Section eyebrow pattern (used by every section):
```html
<div class="section-eyebrow">[LABEL]</div>
<div class="section-title">[Title]</div>
<div class="section-sub">[Subtitle text]</div>
```

Existing General Notes mention (OneNote feature card, line 689-692):
```html
<div class="feature-cell">
  <div class="feature-icon">&#128214;</div>
  <h3>OneNote Integration</h3>
  <p>Auto-creates structured OneNote pages organized by role: Tenant, Landlord, Buyer, Seller, or Investor. Qualifying questions pre-filled. Plus General Notes — jot freeform notes mid-call and push them to OneNote with one click.</p>
</div>
```

Existing Agent-to-Agent card in Forms section (line 987-993):
```html
<div style="...">
  <div style="...">
    <div style="...">&#129309;</div>
    <h3 style="...">Agent-to-Agent</h3>
  </div>
  <p style="...">Commission split agreements (Form I) for co-broker deals across all transaction types.</p>
</div>
```

Area Guides section (lines 1050-1249, ~200 lines):
  - Section with id="areas"
  - 10 community profile cards in 2-column grid
  - Stats row (10 Areas, Live Market Stats, 3.5-9% Yield Range, All Freehold)
  - Market data disclaimer paragraph

CSS responsive rules targeting #areas (lines 560-561, 567):
```css
/* At 900px breakpoint: */
#areas .section-center > div[style*="grid-template-columns: repeat(2"] { grid-template-columns: 1fr !important; }
#areas .section-center > div[style*="grid-template-columns: repeat(4"] { grid-template-columns: repeat(2, 1fr) !important; }
/* At 600px breakpoint: */
#areas .section-center > div[style*="grid-template-columns: repeat(4"] { grid-template-columns: 1fr !important; }
```

Section background alternation (current order, white = default, gray = section-alt):
  HERO (white) -> DEMO (gray) -> FEATURES (gray, section-alt) -> SCREENSHOTS (white)
  -> TEMPLATES (gray, section-alt) -> TRANSCRIBER (white) -> FORMS (gray, section-alt)
  -> AREA GUIDES (white) -> EDUCATION (gray, section-alt) -> FAQ (white) -> GETTING STARTED (gray)
  -> DOWNLOAD (white) -> CTA (white)

CSS design system variables:
```css
:root {
  --navy: #111111; --ice: #f8f8f8; --cloud: #f5f5f5;
  --heading: #111111; --text: #333333; --text-light: #666666;
  --text-muted: #999999; --border: #e0e0e0; --border-light: #f0f0f0;
}
```
Fonts: 'DM Serif Display' for headings, 'DM Sans' for body.

docs/index.html is 1,234 lines (NOT identical to landing/index.html).
Per user decision: after edits, copy landing/index.html wholesale to docs/index.html.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing page content -- General Notes section, Area Guides trim, nav and CSS cleanup</name>
  <files>landing/index.html</files>
  <action>
Make three coordinated edits to landing/index.html:

**1. Expand General Notes into a dedicated feature callout (LAND-01)**

The existing OneNote feature card (line 689-692) already mentions General Notes in one line appended to the OneNote card. Per user decision, General Notes needs "a section describing" the feature. However, per research recommendation, a full standalone section (like Transcriber gets) would be disproportionate for a textarea + push button. The right approach is:

- Split the OneNote feature card into two separate feature cards in the Features Grid:
  - Card 1: "OneNote Integration" -- keep the existing role-based structured pages description WITHOUT the General Notes sentence
  - Card 2: "General Notes" -- a NEW feature card dedicated to General Notes

The new General Notes feature card content:
```html
<div class="feature-cell">
  <div class="feature-icon">&#128221;</div>
  <h3>General Notes</h3>
  <p>Jot freeform notes mid-call and push them to the contact's OneNote page with one click. Each note is timestamped automatically. Nothing is stored locally — OneNote is your single source of truth.</p>
</div>
```

Place it directly AFTER the OneNote Integration card (so the two note-taking features are adjacent).

Update the OneNote Integration card to remove the "Plus General Notes" sentence:
```html
<div class="feature-cell">
  <div class="feature-icon">&#128214;</div>
  <h3>OneNote Integration</h3>
  <p>Auto-creates structured OneNote pages organized by role: Tenant, Landlord, Buyer, Seller, or Investor. Qualifying questions pre-filled so you never miss a detail.</p>
</div>
```

This adds one feature card to the grid (14 becomes 15 cards). The 4-column grid handles odd counts gracefully.

**2. Verify agent-to-agent mention is sufficient (LAND-02)**

The existing Forms section already has a dedicated Agent-to-Agent card (lines 987-993) with: "Commission split agreements (Form I) for co-broker deals across all transaction types." The Forms feature card (line 696) also says "agent-to-agent commission forms." These existing mentions satisfy LAND-02. No changes needed for LAND-02.

**3. Trim Area Guides section (lines 1050-1249)**

Replace the entire Area Guides section (from `<!-- AREA GUIDES -->` through `</section>` before `<!-- EDUCATION -->`) with a compact standalone section that keeps the `id="areas"` anchor:

```html
<!-- AREA GUIDES -->
<section class="section" id="areas">
  <div class="section-center">
    <div class="section-eyebrow">Area Guides</div>
    <div class="section-title">10 Dubai communities at your fingertips</div>
    <div class="section-sub">In-depth profiles for Downtown, Marina, Palm Jumeirah, Business Bay, Dubai Hills, JBR, Creek Harbour, JVC, MBR City, and Dubai South. Market stats, lifestyle insights, and investment outlook — all built into the app.</div>
  </div>
</section>
```

This removes ~190 lines of community profile cards and replaces them with a ~7-line brief mention. The section retains `id="areas"` so the nav link still works. The section retains `class="section"` (no `section-alt`) which is the correct white background to maintain alternation: FORMS (gray) -> AREA GUIDES (white) -> EDUCATION (gray).

**4. Clean up CSS responsive rules**

Remove the three CSS rules that target the now-deleted `#areas` grid layouts:
- Line ~560: `#areas .section-center > div[style*="grid-template-columns: repeat(2"] { grid-template-columns: 1fr !important; }`
- Line ~561: `#areas .section-center > div[style*="grid-template-columns: repeat(4"] { grid-template-columns: repeat(2, 1fr) !important; }`
- Line ~567: `#areas .section-center > div[style*="grid-template-columns: repeat(4"] { grid-template-columns: 1fr !important; }`

These rules targeted inline-style grids in the old Area Guides section. The new compact section has no inline grid styles, so these rules are dead code.

**5. Verify section background alternation**

After changes, confirm the alternation from top to bottom. The removal of ~190 lines does NOT change the section count (Area Guides section remains, just shorter), so the white/gray rhythm should be preserved automatically. But verify: FORMS (section-alt/gray) -> AREA GUIDES (white) -> EDUCATION (section-alt/gray). If any adjacent sections have the same background, add or remove `section-alt` class to fix.

**Anti-patterns to avoid:**
- Do NOT add any external libraries or build tools -- this is a zero-dependency HTML page.
- Do NOT change the existing copywriting tone -- match the DM Sans body text style used throughout.
- Do NOT add screenshots or visual assets that don't exist yet.
- Do NOT mention Quick Calculators (feature doesn't exist yet, per deferred decisions).
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Real Estate Ecosystem" && node -e "const fs=require('fs'); const h=fs.readFileSync('landing/index.html','utf8'); const checks=[]; checks.push(['General Notes card exists', /General Notes<\/h3>/.test(h)]); checks.push(['General Notes privacy mention', /nothing.*stored locally|single source of truth/i.test(h)]); checks.push(['OneNote card no longer has Plus General Notes', !/Plus General Notes/.test(h)]); checks.push(['Old area guide cards removed', !/Downtown Dubai<\/h3>/.test(h)]); checks.push(['Area guides brief mention exists', /10 Dubai communities/.test(h)]); checks.push(['areas anchor still exists', /id=\"areas\"/.test(h)]); checks.push(['Agent-to-Agent card still exists', /Commission split agreements/.test(h)]); checks.push(['No dead #areas CSS rules', !/#areas .section-center > div/.test(h)]); checks.push(['Nav areas link intact', /href=\"#areas\"/.test(h)]); let pass=true; checks.forEach(([name,ok])=>{console.log((ok?'PASS':'FAIL')+': '+name); if(!ok)pass=false;}); process.exit(pass?0:1);"</automated>
  </verify>
  <done>
    - General Notes has its own dedicated feature card in the Features Grid with privacy emphasis and one-click push description
    - OneNote Integration card is updated (no longer contains General Notes sentence)
    - Agent-to-Agent content in Forms section is preserved (LAND-02 satisfied)
    - Area Guides section is trimmed to a brief eyebrow+title+subtitle mention (~7 lines replaces ~200 lines)
    - areas nav anchor still works
    - Dead CSS responsive rules targeting old #areas grid are removed
    - Section background alternation is consistent
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync docs/index.html and validate both files</name>
  <files>docs/index.html</files>
  <action>
Copy landing/index.html to docs/index.html wholesale. Per user decision, "landing/ and docs/ must stay in sync." The docs/ version was already out of sync (1,234 lines vs 1,657 lines in landing/), so a full overwrite is the correct approach.

After copying, verify:
1. The two files are identical (diff produces no output)
2. All anchor links in nav have matching section IDs in the page
3. No references to "10 Areas" stats remain in the page (the old stats row was removed with the full area guides)
4. The word "General Notes" appears in the page (confirming LAND-01 content survived the copy)

Also check for any other stale number references:
- The Features Grid now has 15 feature cards -- but the page never states a feature count, so no update needed
- The Education section says "1,500+ flashcards" and "10 decks" -- these are accurate per project state (1,517 cards, 10 decks)
- Forms stats row says "20+ Forms", "4 Categories", "1-Click Send" -- all still accurate
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Real Estate Ecosystem" && diff landing/index.html docs/index.html > /dev/null 2>&1 && echo "PASS: files are identical" || (echo "FAIL: files differ"; exit 1)</automated>
  </verify>
  <done>
    - docs/index.html is an exact copy of landing/index.html
    - diff between the two files produces zero output
    - No stale stats or number references remain
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `General Notes` appears as a dedicated feature card in the Features Grid (not just a mention inside OneNote card)
2. Agent-to-Agent content in Forms section is intact
3. Area Guides section is a brief mention (~7 lines), not 10 full profile cards (~200 lines)
4. All nav links point to valid section IDs
5. Section background alternation is consistent (no two adjacent sections with same background)
6. landing/index.html and docs/index.html are byte-identical
7. No dead CSS rules targeting removed DOM elements
</verification>

<success_criteria>
- LAND-01: Landing page has a dedicated General Notes feature card emphasizing ephemeral notes, one-click push to OneNote, timestamped entries, and no local storage (privacy)
- LAND-02: Landing page mentions agent-to-agent collaboration tools via existing Forms section content (Agent-to-Agent card with Form I commission split description)
- Area Guides section reduced from ~200 lines of community profiles to ~7-line brief feature mention
- Both landing/index.html and docs/index.html contain identical content
- Page renders correctly with no broken nav links or inconsistent section backgrounds
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-update/07-01-SUMMARY.md`
</output>
