---
phase: 03-secondary-features-and-website
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - electron-builder.config.js
  - landing/index.html
  - landing/AgentKit-Setup-Guide.html
autonomous: false
requirements: [WEB-01]
user_setup:
  - service: github
    why: "GitHub Pages hosting for landing site + GitHub Releases for installer downloads"
    env_vars:
      - name: GH_TOKEN
        source: "GitHub -> Settings -> Developer Settings -> Personal Access Tokens -> Generate new token (repo scope)"
    dashboard_config:
      - task: "Enable GitHub Pages"
        location: "GitHub Repo -> Settings -> Pages -> Source: Deploy from branch (gh-pages, root)"
      - task: "Verify repo visibility"
        location: "GitHub Repo -> Settings -> General -> Danger Zone -> Change visibility (must be public for free GitHub Pages)"

must_haves:
  truths:
    - "Landing site download buttons point to working GitHub Releases URLs"
    - "electron-builder is configured to publish to GitHub Releases"
    - "Landing page download links use /releases/latest/download/ pattern so they auto-resolve to newest version"
  artifacts:
    - path: "electron-builder.config.js"
      provides: "GitHub Releases publish configuration"
      contains: "provider.*github"
    - path: "landing/index.html"
      provides: "Download buttons with GitHub Releases URLs"
      contains: "releases/latest/download"
  key_links:
    - from: "landing/index.html"
      to: "GitHub Releases"
      via: "href pointing to /releases/latest/download/"
      pattern: "releases/latest/download"
    - from: "electron-builder.config.js"
      to: "GitHub Releases API"
      via: "publish.provider = 'github'"
      pattern: "provider.*github"
---

<objective>
Configure electron-builder for GitHub Releases publishing, update landing page download buttons with working URLs, and prepare for GitHub Pages deployment.

Purpose: The public website needs working download links so agents (or their managers) can find, download, and install the app. This is the WEB-01 requirement.
Output: electron-builder publish config, landing page with correct download URLs, GitHub Pages deployment instructions.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-secondary-features-and-website/03-RESEARCH.md

@electron-builder.config.js
@landing/index.html

<interfaces>
<!-- Existing electron-builder config structure -->

From electron-builder.config.js:
```javascript
module.exports = {
  appId: 'ae.agentkit.app',
  productName: 'Agent Kit',
  // ... directories, files, win, nsis, mac configs ...
  publish: null,  // Currently disabled — needs to be set to GitHub provider
}
```

From landing/index.html:
```html
<!-- Current download button (line 679) — points to local release folder: -->
<a href="../release/Agent Kit Setup 1.0.0.exe" class="btn-download-sm" download>Download .exe</a>

<!-- Current main download button (line 979) — same local path: -->
<a href="../release/Agent Kit Setup 1.0.0.exe" class="btn-download" download>Download .exe</a>

<!-- macOS button (line 985) — disabled placeholder: -->
<a href="#" class="btn-download" style="opacity: 0.4; pointer-events: none;">Coming Soon</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure electron-builder publish and update landing page download URLs</name>
  <files>electron-builder.config.js, landing/index.html, landing/AgentKit-Setup-Guide.html</files>
  <action>
**1. Update `electron-builder.config.js`:**
- Change `publish: null` to:
```javascript
publish: {
  provider: 'github',
  owner: 'OWNER',     // placeholder — user fills in their GitHub username
  repo: 'REPO',       // placeholder — user fills in their repo name
  releaseType: 'draft' // draft during development; change to 'release' for production
}
```
- Add `artifactName` to the top-level config to ensure consistent filenames across versions:
  - For Windows: in the `nsis` block, add `artifactName: 'AgentKit-Setup.${ext}'`
  - Alternatively, add top-level `artifactName: 'AgentKit-Setup.${ext}'` (works for NSIS)
  - For mac dmg, either keep default or set via mac target config
- The goal: download URLs like `/releases/latest/download/AgentKit-Setup.exe` stay valid across versions

**2. Update `landing/index.html` download buttons:**

Find all download button `href` values that currently point to `../release/Agent Kit Setup 1.0.0.exe` and replace with GitHub Releases URL pattern.

There are 3 download links to update:
- Line ~679 (Getting Started section, Windows): Change `href="../release/Agent Kit Setup 1.0.0.exe"` to `href="https://github.com/OWNER/REPO/releases/latest/download/AgentKit-Setup.exe"` and remove the `download` attribute (external URL, browser handles it)
- Line ~979 (Download section, Windows): Same change
- Line ~985 (Download section, macOS): Change from disabled placeholder to `href="https://github.com/OWNER/REPO/releases/latest/download/AgentKit.dmg"` — keep the same styling but make it clickable (remove `opacity: 0.4; pointer-events: none` and change text from "Coming Soon" to "Download .dmg")

Also in the hero section:
- Line ~554: The hero "Download for Windows" button links to `#download` anchor — this is fine, leave it

**Use placeholder values** `OWNER` and `REPO` in all URLs. Add an HTML comment near the first download link:
```html
<!-- TODO: Replace OWNER/REPO with your GitHub username and repository name -->
```

**3. Check `landing/AgentKit-Setup-Guide.html`:**
- If it contains any download links pointing to local paths, update those too with the same GitHub Releases URL pattern
- If no download links exist in the setup guide, skip this file

**4. Remove stale `download` attributes:**
- The `download` attribute on `<a>` tags only works for same-origin URLs. Since GitHub Releases URLs are cross-origin, remove `download` from all updated links.

**IMPORTANT:** Keep the existing landing page design, CSS, and layout EXACTLY as-is (per locked decision: "existing 4 HTML pages are fine"). Only change `href` values and the macOS button state.
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Real Estate Ecosystem" && grep -c "releases/latest/download" landing/index.html && grep -c "provider" electron-builder.config.js</automated>
  </verify>
  <done>electron-builder configured with GitHub Releases publish provider. All 3 landing page download buttons point to /releases/latest/download/ URLs with consistent artifact names. macOS button is active (no longer "Coming Soon").</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify download links and builder config</name>
  <what-built>
  - electron-builder.config.js now has GitHub Releases publish configuration with OWNER/REPO placeholders
  - Landing page download buttons updated from local paths to GitHub Releases /latest/download/ URLs
  - macOS download button activated (no longer "Coming Soon")
  - Artifact naming configured for consistent URLs across versions
  </what-built>
  <how-to-verify>
  1. Open `electron-builder.config.js` and verify the publish block has `provider: 'github'`
  2. Open `landing/index.html` in a browser and inspect the download buttons:
     - Windows .exe button should have href containing `releases/latest/download/AgentKit-Setup.exe`
     - macOS .dmg button should be clickable (not grayed out) with href containing `releases/latest/download/AgentKit.dmg`
  3. Confirm the existing page design, colors, and layout are unchanged
  4. Decide: Replace OWNER/REPO placeholders with your actual GitHub username and repo name now, or defer to first release
  5. When ready to deploy the site: push `landing/` contents to a `gh-pages` branch and enable GitHub Pages in repo Settings (Source: Deploy from branch, Branch: gh-pages, Folder: / root)
  </how-to-verify>
  <resume-signal>Type "approved" to complete, or describe any issues with the URLs or config</resume-signal>
</task>

</tasks>

<verification>
1. electron-builder.config.js has `publish.provider` set to `'github'`
2. Landing page has 3 download links pointing to `releases/latest/download/` pattern
3. No download links point to local `../release/` paths
4. macOS button is active and functional
5. Page design preserved (no CSS, layout, or content changes beyond download buttons)
</verification>

<success_criteria>
- electron-builder can publish to GitHub Releases (config in place, user provides GH_TOKEN and fills OWNER/REPO)
- Landing page download buttons use version-independent URLs that auto-resolve to latest release
- Both Windows and macOS download buttons are active
- Existing landing page design is preserved exactly
- User knows next steps for GitHub Pages deployment
</success_criteria>

<output>
After completion, create `.planning/phases/03-secondary-features-and-website/03-03-SUMMARY.md`
</output>
