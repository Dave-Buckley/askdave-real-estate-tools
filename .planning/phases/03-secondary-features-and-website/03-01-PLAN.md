---
phase: 03-secondary-features-and-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/checklists.ts
  - src/main/contacts.ts
  - src/main/ipc.ts
  - src/main/store.ts
  - src/preload/index.ts
  - src/renderer/panel/components/ContactCard.tsx
  - src/renderer/settings/components/FeatureToggles.tsx
autonomous: true
requirements: [ORG-03]

must_haves:
  truths:
    - "Agent opens a contact, selects a transaction type, and sees the correct document checklist"
    - "Agent can tick a document and the timestamp is recorded and persisted"
    - "Agent can untick a document and the timestamp is removed"
    - "Checklist progress is visible at a glance (e.g., 3/9 documents)"
    - "Checklist can be enabled/disabled via settings toggle"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "TransactionType, ChecklistItem, ContactChecklist types; updated Contact interface"
      contains: "TransactionType"
    - path: "src/shared/checklists.ts"
      provides: "Static UAE document checklist definitions for tenancy, sale, renewal, off-plan"
      exports: ["TRANSACTION_CHECKLISTS"]
    - path: "src/main/ipc.ts"
      provides: "checklist:save and checklist:get IPC handlers"
      contains: "checklist:save"
    - path: "src/renderer/panel/components/ContactCard.tsx"
      provides: "Collapsible checklist section with transaction type selector, tick/untick, progress"
      min_lines: 50
  key_links:
    - from: "src/renderer/panel/components/ContactCard.tsx"
      to: "src/preload/index.ts"
      via: "window.electronAPI.saveChecklist / getChecklist"
      pattern: "electronAPI\\.saveChecklist"
    - from: "src/main/ipc.ts"
      to: "src/main/contacts.ts"
      via: "upsertContact with checklist field"
      pattern: "checklist:save"
    - from: "src/renderer/panel/components/ContactCard.tsx"
      to: "src/shared/checklists.ts"
      via: "TRANSACTION_CHECKLISTS import for static list merge"
      pattern: "TRANSACTION_CHECKLISTS"
---

<objective>
Add document checklist feature per client, configured by transaction type (tenancy, sale, renewal, off-plan) with tick + ISO timestamp tracking.

Purpose: Agents need to track which documents have been collected for each client's transaction. This is the core ORG-03 requirement.
Output: Working checklist UI in ContactCard, persisted via electron-store, with feature toggle.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-secondary-features-and-website/03-RESEARCH.md

@src/shared/types.ts
@src/main/contacts.ts
@src/main/ipc.ts
@src/main/store.ts
@src/preload/index.ts
@src/renderer/panel/components/ContactCard.tsx
@src/renderer/settings/components/FeatureToggles.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/shared/types.ts:
```typescript
export interface Contact {
  e164: string
  displayNumber: string
  name: string
  roles: ContactRole[]
  notes: string
  oneNotePageId?: string
  createdAt: string
  updatedAt: string
}

export interface AppSettings {
  clipboardEnabled: boolean
  hotkeysEnabled: boolean
  // ... other settings ...
  phoneLinkEnabled: boolean
  followUpPromptEnabled: boolean
  templates: Template[]
  contacts: Record<string, Contact>
}
```

From src/main/contacts.ts:
```typescript
export function getContact(e164: string): Contact | null
export function upsertContact(e164: string, data: Partial<Omit<Contact, 'e164' | 'createdAt' | 'updatedAt'>>): Contact
```

From src/preload/index.ts:
```typescript
contextBridge.exposeInMainWorld('electronAPI', {
  // ... existing methods ...
  getContact: (e164: string) => ipcRenderer.invoke('contacts:get', e164),
  upsertContact: (e164: string, data: Record<string, unknown>) => ipcRenderer.invoke('contacts:upsert', e164, data),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define checklist types, static data, and backend wiring</name>
  <files>src/shared/types.ts, src/shared/checklists.ts, src/main/contacts.ts, src/main/ipc.ts, src/main/store.ts, src/preload/index.ts, src/renderer/settings/components/FeatureToggles.tsx</files>
  <action>
**1. Add types to `src/shared/types.ts`:**
- Add `TransactionType` union: `'tenancy' | 'sale' | 'renewal' | 'off-plan'`
- Add `ChecklistItem` interface: `{ id: string; label: string; receivedAt?: string }` — `receivedAt` is an ISO timestamp when ticked, `undefined` when not ticked (per locked decision: tick + timestamp)
- Add `ContactChecklist` interface: `{ transactionType: TransactionType; items: ChecklistItem[]; updatedAt: string }`
- Add `checklist?: ContactChecklist` field to the existing `Contact` interface
- Add `checklistEnabled: boolean` to `AppSettings` interface

**2. Create `src/shared/checklists.ts`:**
- Export `TRANSACTION_CHECKLISTS: Record<TransactionType, { id: string; label: string }[]>` containing the UAE document sets from RESEARCH.md:
  - `tenancy`: 9 items (tenant passport, tenant Emirates ID, tenant visa, landlord passport, landlord Emirates ID, title deed, DEWA premise number, signed tenancy contract, Form A)
  - `renewal`: 5 items (renewed contract, previous Ejari, tenant Emirates ID, landlord passport, DEWA premise number)
  - `sale`: 10 items (seller passport, seller Emirates ID, buyer passport, buyer Emirates ID, title deed original, Form A, Form B, Form F/MOU, NOC from developer, mortgage liability letter)
  - `off-plan`: 6 items (buyer passport, buyer Emirates ID, SPA, payment receipts, Oqood certificate, NOC from developer, Power of Attorney)
- Use the exact IDs and labels from RESEARCH.md Pattern 3

**3. Update `src/main/contacts.ts`:**
- In `upsertContact`, add `checklist` to the merged contact object: `checklist: data.checklist ?? existing?.checklist`

**4. Add IPC handlers in `src/main/ipc.ts`:**
- `ipcMain.handle('checklist:save', (_event, e164: string, checklist: ContactChecklist) => { upsertContact(e164, { checklist }) })` — import `ContactChecklist` from types
- `ipcMain.handle('checklist:get', (_event, e164: string) => { const contact = getContact(e164); return contact?.checklist ?? null })`
- Add the `ContactChecklist` import to the existing import line from `'../shared/types'`

**5. Update `src/main/store.ts`:**
- Add `checklistEnabled: true` to the `defaults` object in the Store constructor

**6. Update `src/preload/index.ts`:**
- Add `saveChecklist: (e164: string, checklist: Record<string, unknown>) => ipcRenderer.invoke('checklist:save', e164, checklist)`
- Add `getChecklist: (e164: string) => ipcRenderer.invoke('checklist:get', e164)`

**7. Update `src/renderer/settings/components/FeatureToggles.tsx`:**
- Add `checklistEnabled: boolean` to `FeatureTogglesProps` interface
- Add `checklistEnabled` to the `values` record
- Add entry to `FEATURES` array: `{ key: 'checklistEnabled', label: 'Document Checklists', description: 'Track required documents per client by transaction type' }`
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Real Estate Ecosystem" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All checklist types defined, static data in place, IPC handlers registered, preload bridge extended, settings toggle added. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build checklist UI in ContactCard</name>
  <files>src/renderer/panel/components/ContactCard.tsx</files>
  <action>
Add a collapsible "Documents" checklist section to ContactCard, gated by a `checklistEnabled` prop.

**Props changes:**
- Add `checklistEnabled: boolean` prop to `ContactCardProps`

**State:**
- `checklistExpanded: boolean` (default `false`) — controls section collapse
- `checklist: ContactChecklist | null` — loaded from store on mount or when e164 changes
- `transactionType: TransactionType | null` — selected transaction type

**Load checklist on mount:**
- `useEffect` on `[e164]`: call `window.electronAPI.getChecklist(e164)`, set `checklist` state. If checklist exists, derive `transactionType` from it.

**Transaction type selector:**
- When no checklist exists yet, show a dropdown or button group to select transaction type (tenancy, sale, renewal, off-plan)
- On selection, generate initial checklist items by importing `TRANSACTION_CHECKLISTS` from `../../shared/checklists` and mapping: `items = TRANSACTION_CHECKLISTS[type].map(item => ({ ...item, receivedAt: undefined }))`
- Save immediately via `window.electronAPI.saveChecklist(e164, { transactionType, items, updatedAt: new Date().toISOString() })`

**Merge logic (per RESEARCH.md anti-pattern guidance):**
- When rendering, always use `TRANSACTION_CHECKLISTS[type]` as source of truth for IDs/labels, then look up `receivedAt` from saved items by matching `id`. This handles cases where static list changes after a code update — new items appear unchecked, removed items silently dropped.

**Checklist rendering:**
- Section header: "Documents" with a progress badge showing `{ticked}/{total}` and a collapse/expand chevron
- When expanded, show each item as a row: checkbox + label + timestamp (formatted as short date, e.g., "Mar 3") if ticked
- Clicking a checkbox toggles `receivedAt`: if unticked, set to `new Date().toISOString()`; if ticked, set to `undefined`
- After each toggle, call `window.electronAPI.saveChecklist(e164, updatedChecklist)`

**Allow changing transaction type:**
- Small "Change type" link/button that resets the checklist to a new type (show confirmation if any items are already ticked)

**Styling (Tailwind):**
- Collapsed section: single-line header with subtle border, matching existing ContactCard section spacing
- Expanded: scrollable if more than 6 items, consistent with existing card padding (p-3, space-y-2 pattern)
- Ticked items: text-green-600 checkmark, muted label, small timestamp
- Unticked items: empty checkbox, normal label

**Position in ContactCard:**
- Add the checklist section AFTER the follow-up reminder buttons and BEFORE the closing `</div>`, gated by `{checklistEnabled && (...)}`

**Update App.tsx:**
- Pass `checklistEnabled={settings.checklistEnabled}` prop to ContactCard (in the existing `<ContactCard ...>` usage)
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Real Estate Ecosystem" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Checklist UI renders in ContactCard: transaction type selector, collapsible document list with tick/untick + timestamps, progress badge, persist on every change. Feature-toggled via checklistEnabled setting.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes with zero errors
2. App builds: `npm run build` succeeds
3. Checklist data model: `TRANSACTION_CHECKLISTS` exports all 4 transaction types with correct item counts (tenancy: 9, renewal: 5, sale: 10, off-plan: 7)
4. IPC round-trip: `checklist:save` and `checklist:get` handlers registered in ipc.ts
5. Preload bridge: `saveChecklist` and `getChecklist` exposed in electronAPI
6. Settings toggle: `checklistEnabled` appears in FeatureToggles with label "Document Checklists"
</verification>

<success_criteria>
- Agent can select a transaction type on a contact and see the correct document checklist
- Agent can tick/untick documents with ISO timestamps recorded
- Checklist progress shows "X/Y documents" at a glance
- Checklist persists across app restart (electron-store)
- Feature can be disabled via Settings toggle
</success_criteria>

<output>
After completion, create `.planning/phases/03-secondary-features-and-website/03-01-SUMMARY.md`
</output>
