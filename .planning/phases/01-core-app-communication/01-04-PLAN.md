---
phase: 01-core-app-communication
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/renderer/popup/App.tsx
  - src/renderer/popup/main.tsx
  - src/renderer/popup/components/ActionBar.tsx
  - src/renderer/popup/index.css
  - src/renderer/settings/App.tsx
  - src/renderer/settings/main.tsx
  - src/renderer/settings/components/FeatureToggles.tsx
  - src/renderer/settings/components/HotkeyRecorder.tsx
  - src/renderer/settings/components/WhatsAppModeSelector.tsx
  - src/renderer/settings/index.css
autonomous: true
requirements:
  - APP-02
  - APP-06

must_haves:
  truths:
    - "When agent copies a UAE phone number, a floating action bar appears near the tray with the number and Dial/WhatsApp buttons"
    - "Popup stays visible until agent clicks an action or dismisses it"
    - "If a new number is copied while popup is showing, it replaces the current number"
    - "Agent can open settings and see on/off toggles for each feature"
    - "Agent can customize global hotkeys by recording a new key combination"
    - "Agent can set default WhatsApp mode (desktop or web/phone)"
    - "Settings changes persist across app restarts"
  artifacts:
    - path: "src/renderer/popup/components/ActionBar.tsx"
      provides: "Floating action bar with phone number, Dial button, WhatsApp button, dismiss button"
      min_lines: 30
    - path: "src/renderer/settings/components/FeatureToggles.tsx"
      provides: "Toggle switches for clipboard detection, hotkeys, and other features"
      min_lines: 25
    - path: "src/renderer/settings/components/HotkeyRecorder.tsx"
      provides: "Keyboard shortcut recorder component for customizing hotkeys"
      min_lines: 40
    - path: "src/renderer/settings/components/WhatsAppModeSelector.tsx"
      provides: "Radio button or dropdown for selecting default WhatsApp open mode"
      min_lines: 20
  key_links:
    - from: "src/renderer/popup/components/ActionBar.tsx"
      to: "window.popupAPI.dial"
      via: "Dial button onClick calls popupAPI.dial(e164)"
      pattern: "popupAPI\\.dial"
    - from: "src/renderer/popup/components/ActionBar.tsx"
      to: "window.popupAPI.openWhatsApp"
      via: "WhatsApp button onClick calls popupAPI.openWhatsApp(e164)"
      pattern: "popupAPI\\.openWhatsApp"
    - from: "src/renderer/settings/components/FeatureToggles.tsx"
      to: "window.settingsAPI.saveSettings"
      via: "Toggle change calls settingsAPI.saveSettings with updated setting"
      pattern: "settingsAPI\\.saveSettings"
---

<objective>
Build the clipboard popup floating window (action bar that appears when a phone number is detected) and the settings window (feature toggles, hotkey recorder, WhatsApp mode selector). These are the two remaining UI surfaces.

Purpose: The popup provides zero-friction phone actions — agent copies a number and immediately sees action buttons without pressing any hotkey (APP-02). The settings window lets agents customize the app to their preferences (APP-06).
Output: Working clipboard popup and settings window, both wired to main process via preload APIs.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-app-communication/01-CONTEXT.md
@.planning/phases/01-core-app-communication/01-RESEARCH.md
@.planning/phases/01-core-app-communication/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — preload APIs available to these renderers -->

From window.popupAPI (contextBridge in popup preload):
```typescript
interface PopupAPI {
  dial: (e164: string) => void
  openWhatsApp: (e164: string) => void
  dismiss: () => void
  onShow: (cb: (e164: string, displayNumber: string) => void) => void
}
```

From window.settingsAPI (contextBridge in settings preload):
```typescript
interface SettingsAPI {
  getSettings: () => Promise<AppSettings>
  saveSettings: (settings: Partial<AppSettings>) => Promise<void>
}
```

From src/shared/types.ts:
```typescript
export interface AppSettings {
  clipboardEnabled: boolean
  hotkeysEnabled: boolean
  dialHotkey: string
  whatsappHotkey: string
  whatsappMode: 'web' | 'desktop'
  templates: Template[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clipboard popup floating action bar</name>
  <files>src/renderer/popup/App.tsx, src/renderer/popup/main.tsx, src/renderer/popup/components/ActionBar.tsx, src/renderer/popup/index.css</files>
  <action>
    1. **src/renderer/popup/main.tsx** — Entry point. Render `<App />`. Import `index.css` with Tailwind.

    2. **src/renderer/popup/index.css** — Import Tailwind: `@import "tailwindcss";`. Set `body { background: transparent; margin: 0; }` since this is a transparent frameless window.

    3. **src/renderer/popup/App.tsx**:
       - State: `phoneNumber: { e164: string, display: string } | null`
       - On mount: subscribe to `window.popupAPI.onShow` — when fired, set phoneNumber state
       - When phone number is set, render `<ActionBar />`
       - When no phone number, render nothing (transparent window)

    4. **src/renderer/popup/components/ActionBar.tsx** (per CONTEXT.md — "Floating action bar style"):
       - Layout: Compact horizontal bar, single row
       - Contents (left to right):
         a. Phone number displayed (formatted, e.g., "+971 50 123 4567") — truncated if needed
         b. **Dial** button (phone icon) — calls `window.popupAPI.dial(e164)`
         c. **WhatsApp** button (WhatsApp icon, green) — calls `window.popupAPI.openWhatsApp(e164)`. Uses stored default preference (main process reads from store).
         d. **Dismiss** button (X icon, subtle) — calls `window.popupAPI.dismiss()`
       - Behavior:
         - Stays visible until agent clicks an action or dismisses (per CONTEXT.md)
         - When an action is clicked (Dial or WhatsApp), the popup auto-dismisses after the action fires
         - If `popupAPI.onShow` fires again while visible, replace the number (per CONTEXT.md: "one popup at a time")
       - Styling:
         - Compact: approximately 320px wide, ~50-60px tall
         - Rounded corners, subtle shadow for floating effect
         - Semi-transparent or white background with slight opacity — feels lightweight (per CONTEXT.md: "lightweight floating toolbar")
         - NOT a modal, NOT a notification toast — a tool bar
         - Buttons have hover states
         - Text size: small but readable (text-sm)

    5. Ensure the popup window itself is transparent and frameless (set in tray.ts during Plan 02). The ActionBar component provides the visual border/background via CSS.
  </action>
  <verify>
    <automated>npx electron-vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Popup renders as a compact floating action bar when a UAE phone number is copied. Shows formatted number + Dial + WhatsApp + Dismiss buttons. Clicking Dial opens dialler and dismisses. Clicking WhatsApp opens chat and dismisses. Clicking X dismisses. Copying a new number replaces the current one. Transparent frameless window styled as a lightweight toolbar.</done>
</task>

<task type="auto">
  <name>Task 2: Settings window with feature toggles, hotkey recorder, and WhatsApp mode</name>
  <files>src/renderer/settings/App.tsx, src/renderer/settings/main.tsx, src/renderer/settings/components/FeatureToggles.tsx, src/renderer/settings/components/HotkeyRecorder.tsx, src/renderer/settings/components/WhatsAppModeSelector.tsx, src/renderer/settings/index.css</files>
  <action>
    1. **src/renderer/settings/main.tsx** — Entry point. Render `<App />`. Import `index.css` with Tailwind.

    2. **src/renderer/settings/index.css** — Import Tailwind: `@import "tailwindcss";`.

    3. **src/renderer/settings/App.tsx** (per CONTEXT.md — "Settings panel opens as a separate window"):
       - State: `settings: AppSettings | null`, `loading: boolean`, `saved: boolean`
       - On mount: load settings via `window.settingsAPI.getSettings()`
       - Layout: Standard window with title "Settings", organized in sections:
         a. **Features** section — `<FeatureToggles />`
         b. **Hotkeys** section — `<HotkeyRecorder />` for dial and WhatsApp
         c. **WhatsApp** section — `<WhatsAppModeSelector />`
       - Auto-save on change: when any setting changes, call `window.settingsAPI.saveSettings(changedSettings)`. Show a brief "Saved" indicator.
       - Alternatively: explicit "Save" button at bottom. Either approach works (Claude's discretion).

    4. **src/renderer/settings/components/FeatureToggles.tsx** (APP-06):
       - List of toggle switches (per CONTEXT.md — "Simple on/off toggle list"):
         a. **Clipboard Detection** — `clipboardEnabled` — "Automatically detect phone numbers when you copy them"
         b. **Global Hotkeys** — `hotkeysEnabled` — "Use keyboard shortcuts to quickly dial or open WhatsApp"
       - Each toggle: label + description + switch control
       - Toggle change calls parent's update handler which saves to store
       - Clean visual design: aligned labels, consistent spacing

    5. **src/renderer/settings/components/HotkeyRecorder.tsx** (APP-01, APP-06):
       - Shows current hotkey for Dial and WhatsApp
       - "Change" button next to each hotkey
       - When "Change" is clicked:
         a. Show "Press new shortcut..." prompt
         b. Listen for keydown event
         c. Capture modifier keys + key (e.g., Ctrl+Shift+D)
         d. Convert to Electron accelerator format (e.g., "CommandOrControl+Shift+D")
         e. Display the captured shortcut
         f. "Save" / "Cancel" buttons to confirm or revert
       - Handle edge cases: Escape to cancel, modifier-only combos rejected, duplicate detection
       - Display hotkeys in human-readable format: "Ctrl+Shift+D" on Windows, "Cmd+Shift+D" on macOS

       NOTE: The actual re-registration of hotkeys happens in the main process when settings change. The settings window only persists the new value — the main process should watch for store changes (or the settings window can send an IPC message to re-register). For this plan, just save the setting value. The main process re-registration is handled by Plan 02's store change listener or a future gap closure if needed.

    6. **src/renderer/settings/components/WhatsAppModeSelector.tsx** (COMM-02):
       - Two options (radio buttons or segmented control):
         a. **Web / Phone** (`'web'`) — "Opens WhatsApp in your browser (works on all devices)" — DEFAULT
         b. **Desktop App** (`'desktop'`) — "Opens WhatsApp desktop app directly (must be installed)"
       - Brief description under each option explaining behavior
       - Selected value saved as `whatsappMode` in settings

    7. Styling:
       - Standard window width (~450px), reasonable height
       - Organized sections with headings and dividers
       - Professional, clean look — agents should feel confident changing settings
       - Toggle switches should look modern (not raw HTML checkboxes)
       - Use Tailwind for all styling
  </action>
  <verify>
    <automated>npx electron-vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Settings window opens as a separate window (not in tray dropdown). Shows feature toggles for clipboard detection and hotkeys. Hotkey recorder captures new key combinations in Electron accelerator format. WhatsApp mode selector allows choosing between web and desktop mode. All changes persist via settingsAPI. Clean, professional Tailwind styling.</done>
</task>

</tasks>

<verification>
1. `npx electron-vite build` succeeds
2. Copying a UAE phone number shows floating action bar popup near tray
3. Popup Dial button opens phone dialler
4. Popup WhatsApp button opens WhatsApp chat
5. Popup dismiss button hides the popup
6. Copying a new number while popup visible replaces the number
7. Settings window opens from tray context menu
8. Feature toggles switch on/off and persist
9. Hotkey recorder captures new key combinations
10. WhatsApp mode selector switches between web and desktop
11. Settings survive app restart (persisted in electron-store)
</verification>

<success_criteria>
- Clipboard popup is a compact floating action bar (not a notification or modal)
- Popup appears near tray area with Dial, WhatsApp, and Dismiss buttons
- Popup stays until action or dismiss, replaces on new number
- Settings window is a separate window (not in tray dropdown)
- Feature toggles for clipboard detection and hotkeys
- Hotkey recorder captures and displays Electron accelerator format shortcuts
- WhatsApp mode selector with web (default) and desktop options
- All settings persist via electron-store
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-app-communication/01-04-SUMMARY.md`
</output>
