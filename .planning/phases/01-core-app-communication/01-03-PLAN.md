---
phase: 01-core-app-communication
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/renderer/panel/App.tsx
  - src/renderer/panel/main.tsx
  - src/renderer/panel/components/PhoneInput.tsx
  - src/renderer/panel/components/ContactCard.tsx
  - src/renderer/panel/components/TemplateList.tsx
  - src/renderer/panel/components/TemplatePreview.tsx
  - src/renderer/panel/components/TemplateEditor.tsx
  - src/renderer/panel/index.css
autonomous: true
requirements:
  - COMM-02
  - COMM-03

must_haves:
  truths:
    - "Agent can type or paste a phone number in the panel and see it formatted with action buttons"
    - "Agent can click Dial to open the phone dialler from the panel"
    - "Agent can click WhatsApp to open a WhatsApp chat from the panel, with one-click default and dropdown for alternative"
    - "Agent can optionally add a contact name for template substitution"
    - "Agent can select a template and see a live preview with {name} filled in"
    - "Agent can click Send via WhatsApp to open WhatsApp with the template message pre-filled"
    - "Agent can click Copy to copy the filled template text to clipboard"
    - "Agent can create, edit, and delete message templates"
  artifacts:
    - path: "src/renderer/panel/App.tsx"
      provides: "Main panel layout — phone input at top, contact card, template list below"
      min_lines: 50
    - path: "src/renderer/panel/components/PhoneInput.tsx"
      provides: "Phone number input field with paste detection and validation feedback"
      min_lines: 30
    - path: "src/renderer/panel/components/ContactCard.tsx"
      provides: "Formatted number display, Dial/WhatsApp action buttons, optional name field"
      min_lines: 40
    - path: "src/renderer/panel/components/TemplateList.tsx"
      provides: "Scrollable list of message templates with select, edit, delete actions"
      min_lines: 30
    - path: "src/renderer/panel/components/TemplatePreview.tsx"
      provides: "Template preview with {name} substitution, Send via WhatsApp and Copy buttons"
      min_lines: 40
  key_links:
    - from: "src/renderer/panel/components/ContactCard.tsx"
      to: "window.electronAPI.dial"
      via: "onClick handler calls electronAPI.dial(e164)"
      pattern: "electronAPI\\.dial"
    - from: "src/renderer/panel/components/ContactCard.tsx"
      to: "window.electronAPI.openWhatsApp"
      via: "onClick handler calls electronAPI.openWhatsApp(e164, mode)"
      pattern: "electronAPI\\.openWhatsApp"
    - from: "src/renderer/panel/components/TemplatePreview.tsx"
      to: "window.electronAPI.sendWhatsAppMessage"
      via: "Send via WhatsApp button calls electronAPI.sendWhatsAppMessage"
      pattern: "electronAPI\\.sendWhatsAppMessage"
    - from: "src/renderer/panel/App.tsx"
      to: "window.electronAPI.onPhoneDetected"
      via: "useEffect subscribes to phone:detected events from clipboard"
      pattern: "electronAPI\\.onPhoneDetected"
---

<objective>
Build the tray panel React UI — the main daily-use interface for agents. Includes phone number input, contact card with action buttons, template list, template preview with name substitution, and template CRUD (create/edit/delete).

Purpose: This is what agents see when they click the tray icon. It must feel like a quick-access tool (per CONTEXT.md: "like Discord/Slack tray menus"), not a full application window. The panel is always useful — phone input and templates are available whether a number is active or not.
Output: Complete tray panel UI with all communication actions and template management.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-app-communication/01-CONTEXT.md
@.planning/phases/01-core-app-communication/01-RESEARCH.md
@.planning/phases/01-core-app-communication/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — preload API available to this renderer -->

From window.electronAPI (contextBridge):
```typescript
interface ElectronAPI {
  // Actions
  dial: (e164: string) => void
  openWhatsApp: (e164: string, mode: 'web' | 'desktop') => void
  sendWhatsAppMessage: (e164: string, message: string) => void

  // Store
  getSettings: () => Promise<AppSettings>
  saveSettings: (settings: Partial<AppSettings>) => Promise<void>
  getTemplates: () => Promise<Template[]>
  saveTemplates: (templates: Template[]) => Promise<void>
  addTemplate: (template: Template) => Promise<Template[]>
  deleteTemplate: (id: string) => Promise<Template[]>
  updateTemplate: (template: Template) => Promise<Template[]>

  // Events
  onPhoneDetected: (cb: (e164: string, displayNumber: string) => void) => void
  removePhoneDetectedListener: () => void
}
```

From src/shared/types.ts:
```typescript
export interface Template { id: string; name: string; body: string; category: string }
export interface AppSettings { clipboardEnabled: boolean; hotkeysEnabled: boolean; dialHotkey: string; whatsappHotkey: string; whatsappMode: 'web' | 'desktop'; templates: Template[] }
export interface ContactInfo { e164: string; displayNumber: string; name?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phone input, contact card, and action buttons</name>
  <files>src/renderer/panel/App.tsx, src/renderer/panel/main.tsx, src/renderer/panel/index.css, src/renderer/panel/components/PhoneInput.tsx, src/renderer/panel/components/ContactCard.tsx</files>
  <action>
    1. **src/renderer/panel/main.tsx** — Entry point. Import React, ReactDOM, render `<App />`. Import `index.css` with Tailwind.

    2. **src/renderer/panel/index.css** — Import Tailwind: `@import "tailwindcss";`. Add any custom styles for the panel (subtle background, compact spacing suitable for a 360x480 tray dropdown).

    3. **src/renderer/panel/App.tsx** — Main panel layout:
       - State: `activeContact: ContactInfo | null`, `contactName: string`, `selectedTemplate: Template | null`, `templates: Template[]`, `settings: AppSettings | null`, `view: 'main' | 'template-editor'`
       - On mount (`useEffect`):
         a. Load templates via `window.electronAPI.getTemplates()`
         b. Load settings via `window.electronAPI.getSettings()`
         c. Subscribe to `window.electronAPI.onPhoneDetected` — when a number is detected via clipboard, auto-populate the active contact
       - Cleanup: call `window.electronAPI.removePhoneDetectedListener()` on unmount
       - Layout (per CONTEXT.md decision — "quick actions hub"):
         a. Top: `<PhoneInput />` — always visible
         b. Middle: `<ContactCard />` — shown when a number is active
         c. Bottom: `<TemplateList />` — always visible, scrollable
       - When a template is selected, show `<TemplatePreview />` overlay/section

    4. **src/renderer/panel/components/PhoneInput.tsx**:
       - Text input field with placeholder "Enter or paste a phone number"
       - On change: attempt to normalize via a lightweight client-side check (just format display — actual normalization happens in main process via IPC for actions)
       - On submit (Enter key or button): set as active contact in parent state
       - Paste handling: detect paste event, immediately try to extract number
       - Show validation feedback: green checkmark for valid UAE number, red X for invalid
       - Compact styling — single line input with a small "Go" or arrow button

    5. **src/renderer/panel/components/ContactCard.tsx** (per CONTEXT.md decisions):
       - Shows formatted phone number (e.g., "+971 50 123 4567") prominently
       - Action buttons row:
         a. **Dial** button (phone icon) — calls `window.electronAPI.dial(e164)`
         b. **WhatsApp** button (WhatsApp icon) — primary click uses saved default preference (`settings.whatsappMode`). A small dropdown arrow or secondary click reveals the alternative option ("Open on Desktop" vs "Open on Phone/Web").
       - "Add name" link below the number — small, unobtrusive. Clicking it expands a text input for entering the contact name. Name is optional (per CONTEXT.md: "Name is not required for actions").
       - When name is entered, it displays alongside the number
       - Clear/close button to deactivate the current contact

    6. Style with Tailwind CSS:
       - Panel background: subtle gray or white (light theme for professional look)
       - Buttons: clearly labeled with icons, hover states
       - WhatsApp button: green tint (#25D366 WhatsApp brand color)
       - Dial button: blue tint
       - Compact spacing — this is a 360px wide dropdown, not a full app
       - Use text-sm or text-xs where appropriate to fit content
  </action>
  <verify>
    <automated>npx electron-vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Panel shows phone input at top. Entering a valid UAE number displays a ContactCard with formatted number, Dial button, WhatsApp button (with default preference + alternative dropdown), and optional "Add name" field. Clipboard-detected numbers auto-populate. All action buttons wire to electronAPI calls. Panel fits 360x480 dimensions with clean Tailwind styling.</done>
</task>

<task type="auto">
  <name>Task 2: Template list, preview with substitution, and template CRUD</name>
  <files>src/renderer/panel/components/TemplateList.tsx, src/renderer/panel/components/TemplatePreview.tsx, src/renderer/panel/components/TemplateEditor.tsx, src/renderer/panel/App.tsx</files>
  <action>
    1. **src/renderer/panel/components/TemplateList.tsx** (COMM-03):
       - Renders scrollable list of templates from state
       - Each template item shows: name, category badge, first line preview (truncated)
       - Click a template → selects it (sets `selectedTemplate` in parent)
       - Small edit (pencil) and delete (trash) icon buttons on each row
       - "Add Template" button at bottom or top of list
       - Delete shows a simple confirm dialog before removing
       - Calls `window.electronAPI.deleteTemplate(id)` on confirm
       - If no templates exist, show "No templates. Create one to get started." message

    2. **src/renderer/panel/components/TemplatePreview.tsx** (COMM-03):
       - Shown when a template is selected AND a number is active
       - Shows the template body with `{name}` replaced by the entered contact name. If no name entered, `{name}` placeholder stays visible as a reminder (per CONTEXT.md: "placeholder stays visible as a reminder")
       - Template substitution: simple `body.replace(/\{name\}/g, contactName)` — only when name has content
       - The preview text is editable — agent can make one-off adjustments before sending (per CONTEXT.md: "Agent can review, edit, then click")
       - Two action buttons at bottom:
         a. **"Send via WhatsApp"** (primary, green, prominent) — calls `window.electronAPI.sendWhatsAppMessage(e164, filledMessage)`. This is the main action per CONTEXT.md.
         b. **"Copy"** (secondary, subtle) — copies the filled message text to clipboard. Uses `navigator.clipboard.writeText()`.
       - Back/close button to deselect template and return to list view

    3. **src/renderer/panel/components/TemplateEditor.tsx** (COMM-03):
       - Used for both creating new templates and editing existing ones
       - Fields: name (text input), category (dropdown: introduction, follow-up, viewing, reminder, alert, thank-you, other), body (textarea)
       - Body textarea: shows character count, mention that `{name}` is the placeholder
       - Save button: validates (name required, body required), calls `window.electronAPI.addTemplate()` for new or `window.electronAPI.updateTemplate()` for edit
       - Cancel button returns to list view
       - Generate unique ID for new templates: `crypto.randomUUID()` or `Date.now().toString(36)`

    4. **Update App.tsx** to wire template state management:
       - `view` state toggles between 'main' (list + preview) and 'template-editor'
       - When a template is deleted, update local state from returned array
       - When a template is created/updated, refresh template list
       - Pass all necessary callbacks to child components

    5. Styling considerations:
       - Template list items should be compact — agent scrolls through 5-8 templates quickly
       - Preview should be a clear, readable text area with obvious Send button
       - "Send via WhatsApp" button should be the most visually prominent element in the preview
       - Editor modal/view should fill the panel width
  </action>
  <verify>
    <automated>npx electron-vite build 2>&1 | tail -10</automated>
  </verify>
  <done>Template list shows all templates with select/edit/delete. Selecting a template with an active number shows preview with {name} substituted (or placeholder visible if no name). "Send via WhatsApp" opens WhatsApp with the filled message. "Copy" copies to clipboard. Template editor allows creating and editing templates with name, category, and body fields. All CRUD operations persist via electronAPI.</done>
</task>

</tasks>

<verification>
1. `npx electron-vite build` succeeds
2. Panel opens from tray click showing phone input and template list
3. Typing "0501234567" shows formatted number with Dial and WhatsApp buttons
4. Clicking "Add name" expands name input
5. Selecting a template shows preview with {name} substituted
6. "Send via WhatsApp" opens WhatsApp with filled message
7. "Copy" copies filled text to clipboard
8. Template CRUD: create new template, edit existing, delete with confirmation
9. WhatsApp button has one-click default + dropdown for alternative mode
</verification>

<success_criteria>
- Phone input accepts typed/pasted numbers and shows formatted ContactCard
- ContactCard has Dial and WhatsApp action buttons that call electronAPI
- WhatsApp button uses saved default preference with alternative accessible via dropdown
- Optional name field for template substitution
- Template list renders all templates with select, edit, delete
- Template preview shows {name} substitution (or visible placeholder when no name)
- Preview is editable before sending
- "Send via WhatsApp" is the primary action, "Copy" is secondary
- Template editor supports create and edit with name, category, body fields
- All template changes persist via electron-store
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-app-communication/01-03-SUMMARY.md`
</output>
